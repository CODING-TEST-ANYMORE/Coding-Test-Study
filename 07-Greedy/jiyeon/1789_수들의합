
#  1789 수들의 합

###  포인트 설명
- 10이라는 숫자가 있으면 1+2+3+4로 만들 수 있다. 4개의 수의 합으로 구한 것이 제일 큰 값이다!
- 즉, 1,2,3--- 이렇게 작은 수부터 더하면 제일 큰 N값(서로 다른 N개의 자연수의 합이 S)을 구할 수 있다.
- 자연수 n을 1부터 시작해서 s에서 빼주고 만약 뺀 값이 음수라면
	- s가 9라고 가정
	- 1 + 2 + 3 + 4 < 9 과 같은 경우가 된다면
	- 1 + 2 + 6 = 9 이렇게 되는 것이므로 n-1을 출력하고 break

```python
import sys
input = sys.stdin.readline

s = int(input())
n = 1

while(True):
  if(s-n < 0):
    print(n-1)
    break
  s -= n
  n += 1
```
